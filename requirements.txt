numpy==1.20.3
setuptools==57.0.0

# Required only for testing:
pyproj == 3.0.1
pytest == 6.2.4

# Needed only for shapefile support:
pyshp == 2.1.3

# Required only if the point set reduction algorithm is needed:
scipy == 1.7.0
